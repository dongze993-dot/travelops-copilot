# TravelOps Copilot runs entirely against bundled, deterministic example data.
# No LLM, database, or deployment credential is required for this portfolio demo.

# Optional: place the local mock-CRM SQLite database somewhere else.
# TRAVELOPS_DB_PATH=runtime/travelops_mock_crm.db
